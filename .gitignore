# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Agent runtime workspace (root only — NOT core/openclaw/workspace source)
/workspace/

# OpenClaw / 9Router runtime data (config, secrets, state)
core/openclaw/openclaw-data/
core/9router/9router-data/

# Logs / OS junk
*.log
.DS_Store
Thumbs.db
